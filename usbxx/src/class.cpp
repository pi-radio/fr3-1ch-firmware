#include <usbxx/class.hpp>
#include <usbxx/device.hpp>

using namespace USBXX;

#if 0
USBClass::USBClass(const std::string &_name, uint8_t _code, DeviceBase *_device) :
        name(_name),
        usb_code(_code),
        device(_device)
{
}
#endif
